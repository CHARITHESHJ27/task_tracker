git config --global user.name "cherry "
git config --global user.email "charitheshj@mmail.com"
 git init we use it fot intitialize the project
 git status --To check the git status
Git add . --It add all the files at once 
Git add  file --it add each file
mkdir -it create folder  in the ssytem
dir/a --Shows the files
git  commit -m "changes  i want to do"
git log  --- it shows the  all the commit we did

git remote add origin "repo link"
git branch -M main--- to push into main branch
git push -u origin main ---u define upstream,
Git pull - get the file from the repo
shortcuts:
    